import * as React from 'react';
/** A simple static component to render some text for the landing page. */
declare class Landing extends React.Component {
    render(): JSX.Element;
}
export default Landing;
